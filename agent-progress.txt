# Agent Session Progress Log (agent-progress.txt)
새로운 에이전트 세션이 시작될 때 문맥을 신속히 파악하기 위한 로그입니다. 큰 작업 주기가 끝날 때마다 에이전트가 직접 기록합니다.

---
## [2026-04-01] Analyze v1 재정의 이어받기 (thread `019d4713-161f-78a2-ad1b-df64eabf1a72`)
- **수행 내역**:
  1. `AnalyzeResponse`를 `topic/sentiment/emotion/correlation` 구조로 재정의:
     - 파일: `app/types/mood.py`
     - 보강 타입: `AnalyzeTopic`, `AnalyzeSentimentDistribution`, `AnalyzeEmotionDistribution`, `AnalyzeCorrelation` 추가
  2. `analyze` 파이프라인을 3-branch fan-out/fan-in 구조로 전환:
     - Branch Topic: `extract_topics_with_llm`
     - Branch Sentiment: `analyze_sentiment_with_llm`
     - Branch Emotion: `analyze_emotion_with_llm`
     - fan-in 단계에서 correlation 재조합
     - 파일: `app/service/analyze_service.py`
  3. LLM 내부 타입을 새 스키마에 맞게 갱신:
     - 파일: `app/types/analyze_llm.py`
  4. Emotion 축 라벨 재사용을 위해 `app/types/emotion.py`를 추가하고 `Analyze` 타입에서 참조.
  5. 테스트 갱신:
     - `tests/test_analyze.py`
     - `tests/test_analyze_inspect.py`
     - `tests/test_analyze_service.py`
  6. Streamlit inspect 콘솔의 sentiment 요약 렌더러를 신/구 스키마 모두 처리하도록 보강:
     - 파일: `app/ui/analyze_console.py`
  7. 문서 동기화:
     - `README.md`, `docs/DESIGN.md`, `docs/ARCHITECTURE.md`에 fan-out 구조와 응답 스키마 변경 반영
- **검증 결과**:
  - `.venv/bin/python -m ruff check app/service/analyze_service.py app/types/analyze_llm.py app/types/mood.py app/types/emotion.py app/ui/analyze_console.py tests/test_analyze.py tests/test_analyze_inspect.py tests/test_analyze_service.py` 통과
  - `.venv/bin/python -m pytest tests/test_analyze_service.py tests/test_analyze.py tests/test_analyze_inspect.py -q` 통과 (19 passed)
  - `.venv/bin/python harness/runner/agent_runner.py --mode full` 통과 (55 passed)
---
## [2026-04-01] Dockerize 이슈 #12 종료 동기화 (`feat_ops_dockerize`)
- **수행 내역**:
  1. `feature_list.json`의 `feat_ops_dockerize.passes` 값을 `true`로 전환.
  2. `scripts/sync_feature_issues.py --sync-state --write-feature-file --apply`를 실행해 GitHub Issue #12 상태를 `closed`로 동기화.
  3. feature 메타데이터(`github_issue.state`, `synced_at`)를 최신 상태로 기록.
- **검증 결과**:
  - `gh issue view 12 --json state` 기준 `CLOSED` 확인
  - `.venv/bin/python -m pytest tests/test_sync_feature_issues.py -q` 통과 (6 passed)
---
## [2026-04-01] Dockerize Health-Check 반영 (`GET /healthz` + Dockerfile `HEALTHCHECK`)
- **수행 내역**:
  1. 신규 상태 점검 API `GET /healthz`를 추가:
     - 타입: `app/types/health.py` (`HealthzResponse`, `status="ok"`)
     - 라우트: `app/runtime/health.py`
     - 앱 등록: `app/main.py`
  2. API 이미지에 Docker `HEALTHCHECK`를 추가:
     - 파일: `docker/Dockerfile.api`
     - 방식: Python 표준 라이브러리 `urllib`로 `http://127.0.0.1:${FASTAPI_SERVER_PORT:-8000}/healthz` 호출
     - 파라미터: `interval=30s`, `timeout=5s`, `start-period=20s`, `retries=3`
  3. 신규 테스트 `tests/test_healthz.py`를 추가해 `/healthz` 응답 계약(`200`, `{\"status\":\"ok\"}`)을 검증.
  4. 문서 동기화:
     - `README.md`: Docker health-check 설명과 `/healthz` 검증 절차 추가
     - `docs/DESIGN.md`: 핵심 API 목록에 `GET /healthz` 추가
     - `docs/ARCHITECTURE.md`: Health Check Flow 섹션 추가
- **검증 결과**:
  - `.venv/bin/python -m ruff check .` 통과
  - `.venv/bin/python -m pytest tests/test_healthz.py tests/test_sentiment_turn.py tests/test_env_config.py -q` 통과 (10 passed)
  - `.venv/bin/python harness/runner/agent_runner.py --mode full` 통과 (56 passed)
  - 로컬 환경에 `docker` 바이너리가 없어 컨테이너 런타임 health 상태(`docker ps`의 health) 직접 검증은 미실행
---
## [2026-03-30] Subagent 병렬 점검 반영 (Docker env 경로 정합 + API 버전 계약 보강)
- **수행 내역**:
  1. 코드리뷰 에이전트/서버 개발 에이전트/Docker 전문가 대체 에이전트를 병렬로 구성해 PR #11 변경분을 재점검.
  2. `docs/templates/docker-compose.parent.api.yml`에서 `APP_ENV` 노출 대비 `dev.env` 고정 참조 결함을 수정:
     - `env_file`: `${APP_ENV:-dev}.env`
     - bind mount: `/app/${APP_ENV:-dev}.env`
  3. `README.md` Parent Compose 가이드에 `APP_ENV` 입력 및 `dev`/`prod` 실행 예시를 추가해 템플릿 계약을 문서와 일치시킴.
  4. `LLM_API_VERSION(optional)`을 도입하고 API 버전 우선순위를 `LLM_API_VERSION -> LLM_MODEL_VERSION -> default`로 통일:
     - 대상: `analyze_service`, `sentiment_service`, `evaluate_sentiment_with_judge`
     - 환경설정 응답 타입/로더(`app/types/llm_config.py`, `app/service/llm_config_service.py`) 확장
     - 관련 테스트(`tests/test_env_config.py`, `tests/test_sentiment_service.py`, `tests/test_analyze_service.py`) 보강
  5. `example.env`, `docs/DESIGN.md`, `docs/ARCHITECTURE.md`, `README.md`를 새 계약에 맞게 동기화.
- **검증 결과**:
  - `bash -n scripts/run_api.sh` 통과
  - `.venv/bin/python -m ruff check .` 통과
  - `.venv/bin/python -m pytest tests/test_env_config.py tests/test_sentiment_service.py tests/test_analyze_service.py -q` 통과 (25 passed)
  - `.venv/bin/python harness/runner/agent_runner.py --mode full` 통과 (55 passed)
  - `docker` 바이너리가 없어 컨테이너 런타임 스모크 테스트는 미실행
---
## [2026-03-30] Dockerize 구현 (Parent Compose 템플릿 + FASTAPI_SERVER_PORT 가변)
- **수행 내역**:
  1. `docker/Dockerfile.api`를 추가하여 `uv sync --locked --no-dev` 기반 API 컨테이너 이미지를 구성.
  2. `.dockerignore`를 추가해 `.venv`, 캐시, 테스트/문서 산출물, 로컬 env 파일을 빌드 컨텍스트에서 제외.
  3. `scripts/run_api.sh`에 `FASTAPI_SERVER_PORT`(기본 8000) 입력 및 포트값 검증(숫자/1~65535)을 추가.
  4. 상위 리포지토리 연동용 템플릿 `docs/templates/docker-compose.parent.api.yml`를 추가:
     - `MEETING_MOOD_TRACKER_SUBMODULE_PATH` 기반 `build.context`/`env_file` 참조
     - `FASTAPI_SERVER_PORT` 기반 포트 매핑/컨테이너 실행 포트 동기화
     - 앱이 `dev.env` 파일을 직접 로드하는 구조를 위해 `dev.env -> /app/dev.env` 읽기 전용 마운트 반영
  5. `README.md`, `docs/AGENT_OPERATIONS_GUIDE.md`에 parent compose 템플릿 사용법 및 신규 환경변수 계약을 문서화.
- **검증 결과**:
  - `bash -n scripts/run_api.sh` 통과
  - `.venv/bin/python -m ruff check .` 통과
  - `.venv/bin/python harness/runner/agent_runner.py --mode precommit` 통과
  - `.venv/bin/python harness/runner/agent_runner.py --mode full` 통과 (51 passed)
  - `docker build -f docker/Dockerfile.api .` 실패: 실행 환경에 `docker` 바이너리가 없어 검증 불가 (`command not found`)
---
## [2026-03-30] Dockerize 작업용 Issue Rule 템플릿 추가
- **수행 내역**:
  1. `feature_list.json`에 신규 기능 `feat_ops_dockerize`(`passes: false`)를 추가하고, Dockerize 작업 범위/비범위/체크리스트/검증/완료조건을 담는 `issue_rule`을 정의.
  2. `scripts/sync_feature_issues.py`의 `build_issue_body`를 확장하여 `issue_rule` 필드를 GitHub Issue 본문 섹션으로 자동 렌더링하도록 구현.
  3. `tests/test_sync_feature_issues.py`에 `issue_rule` 렌더링 테스트를 추가해 비문자 항목 필터링과 섹션 생성을 회귀 검증.
  4. `README.md`, `docs/AGENT_OPERATIONS_GUIDE.md`에 `issue_rule` 확장 필드 운영 규칙을 문서화.
- **검증 결과**:
  - `.venv/bin/python -m ruff check scripts/sync_feature_issues.py tests/test_sync_feature_issues.py` 통과
  - `.venv/bin/python -m pytest tests/test_sync_feature_issues.py -q` 통과 (6 passed)
  - `.venv/bin/python scripts/sync_feature_issues.py --create-missing --sync-state` 통과 (dry-run에서 `feat_ops_dockerize` 생성 예정 확인)
---
## [2026-03-30] Analyze 응답 `mood/confidence` -> `sentiment` 구조로 브레이킹 전환
- **수행 내역**:
  1. `app/types/mood.py`에서 `AnalyzeResponse`를 `topic + sentiment` 구조로 교체:
     - `sentiment.positive.confidence`
     - `sentiment.negative.confidence`
     - `sentiment.neutral.confidence`
  2. `AnalyzeSentiment` 모델에 합계 100 검증을 추가하여 타입 레벨에서도 일관성을 강제.
  3. `app/service/analyze_service.py` Stage 2를 `analyze_sentiment_with_llm`로 전환하고 JSON schema를 sentiment 3축 구조로 변경.
  4. 서버 정규화 로직(`_normalize_sentiment_confidences`)을 추가해 음수 클램프 + Largest Remainder + tie-break(positive -> negative -> neutral)로 정수 퍼센트 합계 100을 보장.
  5. 런타임/inspect/SSE 경로가 새 `AnalyzeResponse` 스키마를 그대로 전달하도록 연동.
  6. Streamlit 결과 패널에 sentiment 분포 요약 테이블을 추가해 `result.sentiment`를 명시적으로 표시.
  7. 테스트(`tests/test_analyze.py`, `tests/test_analyze_inspect.py`, `tests/test_analyze_service.py`, `tests/architecture/test_fastapi_contracts.py`)를 전면 갱신.
- **문서 반영**:
  - `README.md`, `docs/DESIGN.md`, `docs/ARCHITECTURE.md`에 새 응답 계약(`sentiment`)과 합계 100 정규화 규칙을 반영.
- **검증 결과**:
  - `.venv/bin/python -m ruff check .` 통과
  - `.venv/bin/python -m pytest tests -v --maxfail=1 --tb=short` 통과 (50 passed)
  - `.venv/bin/python harness/runner/agent_runner.py --mode full` 통과
---
## [2026-03-30] Streamlit SSE 중간 결과 실시간 렌더링 적용
- **수행 내역**:
  1. `app/ui/analyze_console.py`에 SSE 이벤트 제너레이터(`_iter_sse_events`)를 도입해 이벤트를 순차 소비하도록 개선.
  2. `_call_inspect_sse_with_live_render`를 추가해 `start/log/result/done` 이벤트 수신 시점마다 Streamlit placeholder를 즉시 갱신.
  3. 기존 SSE 실패 시 REST fallback 동작은 유지하고, 성공 시에는 기존처럼 최종 응답을 히스토리에 저장.
- **검증 결과**:
  - `.venv/bin/python -m ruff check app/ui/analyze_console.py` 통과
  - `.venv/bin/python -m pytest tests -v --maxfail=1 --tb=short` 통과 (47 passed)
  - `.venv/bin/python harness/runner/agent_runner.py --mode full` 통과
---
## [2026-03-30] Analyze 2-Stage LLM 전환 (Topic ↔ Mood/Confidence 분리 + reasoning_effort 분리)
- **수행 내역**:
  1. `app/service/analyze_service.py`를 2-stage LLM 오케스트레이션으로 재구성.
     - Stage 1: `extract_topics_with_llm` (`response_format=json_schema`, `reasoning_effort=none`)
     - Stage 2: `analyze_mood_confidence_with_llm` (`response_format=json_schema`, `reasoning_effort=minimal`)
  2. 토픽 단계 전처리 함수 `preprocess_for_topic`을 추가(정규표현식 기반 불용어 제거/공백 정리).
  3. 토픽/분위기 단계의 structured 출력 검증 타입을 `app/types/analyze_llm.py`로 분리.
  4. `/api/v1/analyze`, `/api/v1/analyze/inspect`에서 `AnalyzeInferenceError`를 `502`(`ANALYZE_LLM_FAILURE`)로 매핑.
  5. 최종 `AnalyzeResponse.topic`은 topics 리스트를 `\", \"`로 결합해 반환하도록 통일.
  6. 신규 테스트 `tests/test_analyze_service.py`를 추가하여 단계별 추론 강도/프롬프트/실패 케이스를 검증.
  7. 런타임 테스트(`tests/test_analyze.py`, `tests/test_analyze_inspect.py`)를 2-stage/502 정책 기준으로 갱신.
- **문서 반영**:
  - `README.md`, `docs/DESIGN.md`, `docs/ARCHITECTURE.md`를 2-stage LLM + 단계별 `reasoning_effort` 정책으로 동기화.
---
## [2026-03-30] Topic Extraction Logic 구현 완료 (`feat_logic_topic_extraction`)
- **수행 내역**:
  1. `app/service/analyze_service.py`의 `_infer_topic`을 고정값 반환에서 규칙 기반 키워드 스코어링 방식으로 전환.
  2. 한국어/영어 키워드를 함께 탐지하고, 영문 키워드는 단어 경계(`\\b`) 기준 매칭하여 한/영 혼합(code-switching) 문장을 안정적으로 처리.
  3. 토픽 후보(`Architecture`, `Budget`, `Bug & Incident`, `Deployment & Operations`, `Performance`, `Roadmap & Schedule`, `Security`, `Hiring`)를 도메인 우선순위로 관리.
  4. 키워드 미탐지 시 기본 토픽 `General Discussion`을 반환하도록 fallback 추가.
  5. `tests/test_analyze.py`에 예산(code-switching) 케이스와 fallback 케이스를 추가해 TDD로 검증 강화.
- **문서 반영**:
  - `README.md`, `docs/DESIGN.md`, `docs/ARCHITECTURE.md`에 Topic Extraction 규칙 기반 알고리즘을 동기화.
  - `feature_list.json`의 `feat_logic_topic_extraction.passes`를 `true`로 갱신.
---
## [2026-03-30] Codex worktree setup에 feature-issue 자동 동기화 통합
- **수행 내역**:
  1. `scripts/setup_worktree.sh`에 feature 동기화 단계를 추가하여 setup 시 `scripts/sync_feature_issues.py --create-missing --sync-state --write-feature-file --apply`를 자동 실행하도록 통합.
  2. 셸 PATH 보강(`/opt/homebrew/bin`, `~/.local/bin`)을 추가해 Codex 실행 환경에서도 `uv`/`gh` 탐색 실패를 줄이도록 보완.
  3. `GITHUB_TOKEN` 미설정 시 `gh auth token` 기반 자동 토큰 획득을 시도하도록 구현.
  4. 동기화 실패는 경고만 남기고 setup은 계속 진행하도록 처리(개발 환경 부트스트랩 안정성 유지).
  5. `SKIP_FEATURE_ISSUE_SYNC=1` opt-out 스위치를 추가해 필요 시 자동 동기화를 비활성화할 수 있도록 지원.
  6. 반복 setup 시 불필요한 파일 변경을 방지하기 위해 `scripts/sync_feature_issues.py`를 개선하여 메타데이터 변경이 있을 때만 `feature_list.json`을 갱신하도록 수정.
- **문서 반영**:
  - `README.md`, `docs/AGENT_OPERATIONS_GUIDE.md`에 자동 동기화 동작과 우회 옵션을 추가.
---
## [2026-03-30] feature_list ↔ GitHub Issue 동기화 자동화 스크립트 추가
- **수행 내역**:
  1. `scripts/sync_feature_issues.py`를 추가하여 `feature_list.json`과 GitHub Issue를 `feature_id` 기준으로 동기화하는 자동화 경로를 구현.
  2. 동기화 기능: 이슈 조회(PR 제외), `<!-- feature_id:... -->`/`[feat_xxx]` 매핑, 누락 이슈 생성 옵션, `passes` 기반 open/closed 상태 동기화 옵션, `feature_list.json` 메타데이터 기록 옵션.
  3. `tests/test_sync_feature_issues.py`를 추가하여 마커 파싱/PR 필터링/메타데이터 기록 로직 회귀 테스트를 보강.
  4. `README.md`, `docs/AGENT_OPERATIONS_GUIDE.md`에 실행 절차(dry-run → apply)와 운영 표준을 문서화.
- **비고**:
  - 현재 환경에는 `GITHUB_TOKEN`이 설정되어 있지 않아 `--apply` 실변경은 수행하지 못함.
---
## [2026-03-30] pre-commit 훅 uv 탐색 안정화 (리뷰 후 수정)
- **수행 내역**:
  1. 리뷰 중 확인된 결함(훅 실행 환경 PATH에 `uv`가 없을 때 커밋이 불필요하게 차단됨)을 수정.
  2. `scripts/pre_commit_hook.sh`에 `uv` 바이너리 탐색 로직(PATH, `~/.local/bin/uv`, `/opt/homebrew/bin/uv`)을 추가.
  3. `git rev-parse --show-toplevel` 기반으로 저장소 루트에서 runner를 실행하도록 보강.
- **검증 결과**:
  - `bash -n scripts/pre_commit_hook.sh` 통과
  - `env PATH="/usr/bin:/bin" bash scripts/pre_commit_hook.sh` 통과
---
## [2026-03-30] Codex worktree setup 표준화 (worktree별 venv + uv cache 재사용)
- **수행 내역**:
  1. `scripts/setup_worktree.sh`를 추가/강화하여 `UV_PROJECT_ENVIRONMENT=<worktree>/.venv`를 강제하고 `uv venv` + `uv sync --locked` 순서로 표준화.
  2. `UV_CACHE_DIR` 기본값을 `~/.cache/uv`로 고정하여 worktree 간 설치 캐시 재사용 정책을 명시.
  3. `.codex/environments/environment.toml`의 `[setup].script`를 `./scripts/setup_worktree.sh`로 연결.
  4. `AGENTS.md`, `README.md`, `docs/AGENT_OPERATIONS_GUIDE.md`에 동일 정책을 문서화하여 에이전트 반복 실행 방식을 통일.
  5. 스크립트 실행 위치와 무관하게 동작하도록 프로젝트 루트 계산/이동(`SCRIPT_DIR` 기반)과 기존 `.venv` 재사용 분기(`pyvenv.cfg` 체크)를 추가.
- **검증 결과**:
  - `bash -n scripts/setup_worktree.sh` 통과
  - `./scripts/setup_worktree.sh` 통과 (worktree `.venv` 생성 + `uv sync --locked` 완료)
  - `(cd app && ../scripts/setup_worktree.sh)` 통과 (서브디렉터리 실행 시에도 동일 `.venv`/sync 동작)
---
## [2026-03-26] 하네스 인프라 확립, 기록 시스템, 초기 API 스켈레톤 구축
- **수행 내역**:
  1. 기계적 SDD 원칙에 입각한 AST 의존성 체커(`arch_checker.py`), 커스텀 에이전트 린터(`AgentWorkflowLinter`), 래퍼 러너(`agent_runner.py`)를 개발하여 하네스 구축.
  2. 첫 번째 `/api/v1/analyze` 목적의 Pydantic 스키마(`mood.py`) 및 테스트 코드(`test_analyze.py`)를 통과하는 Fast API 뼈대 구현 완수.
  3. "점진적 공개(Progressive Disclosure)" 원칙을 담은 `docs/` 기록 시스템(ARCHITECTURE, DESIGN, QUALITY_SCORE) 신설.
  4. 루트 경로에 에이전트 내비게이션용 `AGENTS.md` (목차화), `feature_list.json`, `agent-progress.txt`, `init.sh` 구비 성공.
- **다음 목표**: `feature_list.json`에 기재된 `feat_logic_topic_extraction` 기능(회의 텍스트를 실질적으로 분석하여 Topic을 도출하는 로직)의 개발.
---
## [2026-03-27] Harness Engineering 고도화 (FastAPI 계약 강제 + Capability Vendor 고정)
- **수행 내역**:
  1. `FastAPIContractValidator`를 신규 도입하여 runtime 라우트의 반환 타입, `response_model`, `app/types` 기반 I/O, dict/list 직접 반환 금지 규칙을 기계적으로 강제.
  2. `agent_runner.py`를 `precommit/full` 모드로 확장하고, 실행 순서를 `architecture -> fastapi-contract -> linter(custom+ruff) -> pytest(full only)`로 표준화.
  3. `scripts/pre_commit_hook.sh`를 runner 단일 진입점으로 통합.
  4. `.agents/vendor/`에 선별 capability(agents/skills) 스냅샷을 고정하고 `capability-manifest.json` + `validate_capability_manifest.py`로 무결성 검사 체계 구축.
  5. `docs/AGENT_OPERATIONS_GUIDE.md`, `docs/AGENT_CAPABILITY_CATALOG.md` 신설 및 `AGENTS.md`, `docs/ARCHITECTURE.md`, `docs/QUALITY_SCORE.md` 문서 동기화.
- **검증 결과**:
  - `uv run ruff check .` 통과
  - `uv run pytest tests -v --maxfail=1 --tb=short` 통과 (13 passed)
  - `uv run python harness/runner/agent_runner.py --mode precommit` 통과
  - `uv run python harness/runner/agent_runner.py --mode full` 통과
  - `uv run python scripts/validate_capability_manifest.py` 통과
---
## [2026-03-27] LLM 설정/환경변수 관리 API 추가 (`GET /api/env/v1`)
- **수행 내역**:
  1. `app/config/llm_env.py`를 신설하여 `python-dotenv` 기반 env 파일 로더 구현 (`APP_ENV=dev|prod`, 기본값 `dev`).
  2. `app/service/llm_config_service.py`를 신설하여 필수 키(`LLM_API_KEY`, `LLM_ENDPOINT`, `LLM_MODEL`) 검증 및 예외 도메인화(검증 실패/로딩 실패) 적용.
  3. `app/types/llm_config.py` 응답 스키마와 `app/runtime/env_config.py` 라우트를 추가하고, `app/main.py`에 라우터 연결.
  4. `tests/test_env_config.py`에 신규 5개 테스트를 추가하여 dev/prod 분기, 원문값 반환, 422/500 에러 경로를 검증.
  5. `.gitignore`(`dev.env`, `prod.env`), `example.env`, `README.md`, `docs/DESIGN.md`, `docs/ARCHITECTURE.md`, `feature_list.json`을 동기화.
  6. `pyproject.toml`, `uv.lock`에 `python-dotenv` 의존성 반영.
- **검증 결과**:
  - `uv run ruff format app/ tests/ docs/` 통과
  - `uv run ruff check .` 통과
  - `uv run pytest tests -v --maxfail=1 --tb=short` 통과 (18 passed)
  - `uv run python harness/runner/agent_runner.py --mode full` 통과
---
## [2026-03-27] 턴 단위 감정분류 API 추가 (`POST /api/v1/sentiment/turn`) + Azure OpenAI SDK 연동
- **수행 내역**:
  1. `app/types/sentiment.py`에 턴 단위 요청/응답 스키마를 정의하고 라벨 집합을 `POS/NEG/NEUTRAL`로 고정.
  2. `app/service/sentiment_service.py`에서 OpenAI Python SDK(`AzureOpenAI`) 기반 감정 분류 로직 구현:
     - env 키(`LLM_API_KEY`, `LLM_ENDPOINT`, `LLM_MODEL_NAME`, `LLM_DEPLOYMENT_NAME`) 로딩
     - `chat.completions.create(..., response_format=json_schema)` 강제
     - JSON 파싱 및 Pydantic 스키마 검증 실패 시 도메인 예외 처리
  3. `app/runtime/sentiment.py` 라우터 및 `app/main.py` 라우터 등록으로 신규 API 오픈.
  4. `app/types/llm_config.py`, `app/service/llm_config_service.py`, `tests/test_env_config.py`, `example.env`를 신 env 키 계약으로 전환 (`LLM_MODEL_VERSION` optional).
  5. 오프라인 품질 평가용 `scripts/evaluate_sentiment_with_judge.py` 배치 스크립트 추가.
  6. `README.md`, `docs/DESIGN.md`, `docs/ARCHITECTURE.md`, `feature_list.json` 동기화.
- **검증 결과**:
  - `uv run ruff check .` 통과
  - `uv run pytest tests -v --maxfail=1 --tb=short` 통과 (25 passed)
  - `uv run python harness/runner/agent_runner.py --mode full` 통과
---
## [2026-03-27] Azure API 버전 설정 분리 (`LLM_API_VERSION`) 반영
- **수행 내역**:
  1. `LLM_MODEL_VERSION`(모델 메타)와 `LLM_API_VERSION`(Azure OpenAI API 스펙 버전)을 분리하여 혼동을 제거.
  2. `app/types/llm_config.py`, `app/service/llm_config_service.py`에 `LLM_API_VERSION(optional)` 필드를 추가.
  3. `app/service/sentiment_service.py`, `scripts/evaluate_sentiment_with_judge.py`가 `LLM_API_VERSION`을 우선 사용하고 미설정 시 기본값 `2025-04-01-preview`를 사용하도록 변경.
  4. `example.env`, `README.md`, `docs/DESIGN.md`, `docs/ARCHITECTURE.md`, `tests/test_env_config.py`, `tests/test_sentiment_service.py` 동기화.
- **검증 결과**:
  - `uv run ruff check .` 통과
  - `uv run pytest tests -v --maxfail=1 --tb=short` 통과 (27 passed)
  - `uv run python harness/runner/agent_runner.py --mode full` 통과
---
## [2026-03-30] Analyze Inspect 확장 (REST + SSE) 및 Streamlit 테스트 UI 추가
- **수행 내역**:
  1. `app/service/analyze_service.py`에 단일 공통 메서드 `run_analyze_pipeline`을 추가하여 `/analyze`, `/analyze/inspect`, `/analyze/inspect/stream`이 동일 알고리즘을 호출하도록 통합.
  2. `app/types/analyze_inspect.py`에 inspect/SSE 전용 타입(`AnalyzeInspectResponse`, `AnalyzeLogicStep`, `AnalyzeLogEntry`, `AnalyzeSseEventPayload`)을 추가.
  3. `app/runtime/analyze.py`에 inspect REST/SSE 라우트를 신규 구현하고, 기존 `/api/v1/analyze`는 기존 응답 계약을 유지하면서 공통 메서드 결과만 반환하도록 변경.
  4. analyze 실행 로그를 메모리 링버퍼(`maxlen=200`)로 저장하는 정책을 서비스 레이어에 적용.
  5. Streamlit UI `app/ui/analyze_console.py`와 실행 스크립트 `scripts/run_ui.sh`를 추가하여 SSE 우선 + REST fallback 표시 흐름 구현.
  6. FastAPI 계약 검사기(`harness/validators/fastapi_contract_checker.py`)를 확장해 허용 경로(`/analyze/inspect/stream`)의 SSE만 예외 허용하고, 나머지 라우트는 기존 response_model 강제를 유지.
  7. 관련 테스트(`tests/test_analyze_inspect.py`, `tests/architecture/test_fastapi_contracts.py`) 및 문서(`README.md`, `docs/ARCHITECTURE.md`, `docs/DESIGN.md`)를 동기화.
  8. 사용자 요청에 따라 `feature_list.json`에 Streamlit inspect UI 기능 항목을 반영.
- **검증 결과**:
  - `python3 -m py_compile app/runtime/analyze.py app/service/analyze_service.py app/types/analyze_inspect.py app/ui/analyze_console.py harness/validators/fastapi_contract_checker.py tests/test_analyze_inspect.py tests/architecture/test_fastapi_contracts.py` 통과
  - 현재 세션 실행 환경에는 `uv`, `ruff`, `pytest`가 설치되어 있지 않아 하네스/테스트 전체 실행은 미수행
